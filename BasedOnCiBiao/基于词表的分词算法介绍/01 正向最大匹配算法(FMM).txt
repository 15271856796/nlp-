#正向最大匹配算法

#分词原理是:单词的颗粒度越大,所能表示的含义就越确切

#算法的实现步骤:①一般从一个字符串的开始位置,最大到底是取多少呢,就是看字典里最长的词的长度,但是如果
                #  不足最大的词长,则选择全部的序列,也就是说去的是词典中的词的最大长度和要分割的句子的长度的两者的最小值
             #  ②首先看该片段是否在词典中,如果是,则算为一个分出来的词,如果不是,则从右边开始,减少一个字符,然后看
             #    短的这个片段是否在词典中,一次循环,逐只剩一个字
            #   ③截取到一个词后,剩下的部分序列再做①②步
